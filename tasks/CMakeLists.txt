# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(command_arbiterTaskLib)
ADD_LIBRARY(${COMMAND_ARBITER_TASKLIB_NAME} SHARED 
    ${COMMAND_ARBITER_TASKLIB_SOURCES})
add_dependencies(${COMMAND_ARBITER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${COMMAND_ARBITER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${COMMAND_ARBITER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${COMMAND_ARBITER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${COMMAND_ARBITER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${COMMAND_ARBITER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${COMMAND_ARBITER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${COMMAND_ARBITER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${COMMAND_ARBITER_TASKLIB_HEADERS}
    DESTINATION include/orocos/command_arbiter)

